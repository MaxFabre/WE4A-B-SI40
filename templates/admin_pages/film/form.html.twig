{% extends 'admin_pages/base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block main %}
    <h1>{{ title }}:</h1>
    {{ form_errors(filmForm) }}
    {{ form(filmForm) }}
{% endblock %}
