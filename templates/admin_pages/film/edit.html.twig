{% extends 'admin_pages/base.html.twig' %}

{% block title %}Éditer {{ film.title }}{% endblock %}

{% block main %}
    <h1>Éditer {{ film.title }}</h1>
    {{ form_errors(filmForm) }}
    {{ form(filmForm) }}
{% endblock %}
